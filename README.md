```bash
sudo apt update && sudo apt install -y git curl openssl systemd && rm -rf ~/self-signed-cert-script && git clone https://github.com/dmsubb/self-signed-cert-script.git && cd self-signed-cert-script && chmod +x 3x-ui.sh && sudo ./3x-ui.sh
